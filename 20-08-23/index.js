// const getParticipants = (handshakes) => {
// 	if(handshakes === 0) return 0;
// 	else if(handshakes === 1) return 2;
// 	else {
// 		let result = 1;
// 		while(result * (result - 1) / 2 < handshakes) {
// 			result++;
// 		}
// 		return result;
// 	}
// }

// const eliminateUnsetBits = (number) => {
//   let newNumber = "";
//   for (let i = 0; i < number.length; i++) {
//     if (number[i] === "1") newNumber += number[i];
//   }
//   if (newNumber.length === 0) return 0;
//   else return parseInt(newNumber, 2);
// };
